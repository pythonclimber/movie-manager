<Page xmlns="http://schemas.nativescript.org/tns.xsd" class="page" navigatingTo="navigatingTo">    
    <StackLayout>
        <GridLayout class="action-bar" columns="*, auto">
            <StackLayout tap="backTap" orientation="horizontal" class="action-wrap" horizontalAlignment="left" verticalAlignment="center">
                <Label text="Back" class="back-text" />
            </StackLayout>
            <StackLayout tap="{{ GoToMyMovies }}" col="1" orientation="horizontal" class="action-wrap" verticalAlignment="center" visibility="{{ Flow == MovieFlowSearch ? 'visible' : 'hidden' }}">
                <Label text="My Movies" class="back-text" />
            </StackLayout>
        </GridLayout>
        <GridLayout>
            <ScrollView>
                <GridLayout rows="auto, auto, *"  visibility="{{ IsLoading ? 'hidden' : 'visible' }}">
                    <GridLayout row="0" columns="auto, *">
                        <GridLayout class="image-layout">
                            <Image src="{{ ImageSource }}" stretch="aspectFill" width="120" height="180" horizontalAlignment="left" verticalAlignment="top" />
                        </GridLayout>
                        <StackLayout class="title-layout" col="1">
                            <Label text="{{ Title + ' (' + Year + ')' }}" class="main-title" textWrap="true" />
                            <Label text="{{ Director }}" />
                            <Label text="{{ Format }}" />
                            <GridLayout tap="{{ AddRating }}" visibility="{{ Flow == MovieFlowCollection && !Wishlist ? 'visible' : 'collapse' }}">
                                <GridLayout columns="auto,auto,auto,auto,auto" visibility="{{ !Rating ? 'collapse' : 'visible' }}">
                                    <Image id="star-1" height="24" width="24" col="0" src="{{ Rating >= 2 ? 'res://star_green_56' : (Rating == 1 ? 'res://star_half_56' : 'res://star_white_56') }}" />
                                    <Image id="star-2" height="24" width="24" col="1" src="{{ Rating >= 4 ? 'res://star_green_56' : (Rating == 3 ? 'res://star_half_56' : 'res://star_white_56') }}" />
                                    <Image id="star-3" height="24" width="24" col="2" src="{{ Rating >= 6 ? 'res://star_green_56' : (Rating == 5 ? 'res://star_half_56' : 'res://star_white_56') }}" />
                                    <Image id="star-4" height="24" width="24" col="3" src="{{ Rating >= 8 ? 'res://star_green_56' : (Rating == 7 ? 'res://star_half_56' : 'res://star_white_56') }}" />
                                    <Image id="star-5" height="24" width="24" col="4" src="{{ Rating >= 10 ? 'res://star_green_56' : (Rating == 9 ? 'res://star_half_56' : 'res://star_white_56') }}" />
                                </GridLayout>
                                <Label color="#009940" text="Click to rate" textDecoration="underline" fontSize="18" visibility="{{ !Rating ? 'visible' : 'collapse' }}" />
                            </GridLayout>
                        </StackLayout>
                    </GridLayout>
                    <GridLayout row="1" columns="*, *">
                        <GridLayout col="0">
                            <Button visibility="{{ !UserId || Wishlist ? 'visible' : 'hidden' }}" text="Add" width="125" height="40" class="btn" android:fontSize="10" ios:fontSize="18" horizontalAlignment="center" tap="{{ AddToMyCollection }}" />
                            <Button visibility="{{ !UserId || Wishlist ? 'hidden' : 'visible' }}" text="Remove" width="125" height="40" class="btn" android:fontSize="10" ios:fontSize="18" horizontalAlignment="center" tap="{{ RemoveFromMyCollection }}" />
                        </GridLayout>
                        <GridLayout col="1">
                            <Button text="Wishlist" width="125" height="40" class="btn" android:fontSize="10" ios:fontSize="18" horizontalAlignment="center" tap="{{ AddToWishlist }}" visibility="{{ !UserId ? 'visible' : 'hidden'}}" />
                            <Button text="Un-wishlist" width="125" height="40" class="btn" android:fontSize="10" ios:fontSize="18" horizontalAlignment="center" tap="{{ RemoveFromMyCollection }}" visibility="{{ UserId && Wishlist ? 'visible' : 'hidden'}}" />
                            <Button text="Edit Formats" width="125" height="40" class="btn" android:fontSize="10" ios:fontSize="18" horizontalAlignment="center" tap="{{ AddFormats }}" visibility="{{ !UserId || Wishlist ? 'hidden' : 'visible' }}" />
                        </GridLayout>
                    </GridLayout>
                    <GridLayout row="2" colSpan="2" class="plot-layout">
                        <Label  text="{{ Plot }}" textWrap="true" />
                    </GridLayout>
                </GridLayout>
            </ScrollView>
            <ActivityIndicator busy="{{ IsLoading || IsPaused }}" color="#009940" row="1" horizontalAlignment="center" verticalAlignment="center" />
        </GridLayout>
    </StackLayout>
</Page>