<Page xmlns="http://schemas.nativescript.org/tns.xsd" navigatingTo="navigatingTo" class="page">
    <GridLayout>
        <GridLayout row="0">
            <ScrollView>
                <ListView items="{{ movies }}">
                    <ListView.itemTemplate>
                        <GridLayout rows="auto" columns="auto, *">
                            <GridLayout col="0" columns="auto" rows="auto" class="favorite-wrap" tap="toggleFavorite">
                                <Image class="icon-image" src="{{ favorite ? 'res://ic_add_to_fav_1' : 'res://ic_add_to_fav' }}" />
                            </GridLayout>
                            <StackLayout col="1" class="info-wrap">
                                <Label text="{{ title }}" class="bold info-bigger" />
                                <Label text="{{ director }}" class="info-orange" />
                            </StackLayout>
                        </GridLayout>
                    </ListView.itemTemplate>
                </ListView>
            </ScrollView>
        </GridLayout>
    </GridLayout>
</Page>
