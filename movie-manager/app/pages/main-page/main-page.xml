<Page xmlns="http://schemas.nativescript.org/tns.xsd" navigatingTo="navigatingTo" class="page">
    <StackLayout>
        <GridLayout class="action-bar" columns="*, auto">
            <StackLayout col="0" tap="searchTap" orientation="horizontal" class="action-wrap" horizontalAlignment="left" verticalAlignment="center">
                <Label text="Search" class="back-text" />
            </StackLayout>
        </GridLayout>
        <GridLayout>
            <GridLayout row="0">
                <ScrollView>
                    <ListView items="{{ movies }}" itemTap="selectMovie" class="movie-list">
                        <ListView.itemTemplate>
                            <GridLayout rows="auto" columns="auto, *">
                                <GridLayout col="0" columns="auto" rows="auto" class="favorite-wrap" tap="toggleFavorite">
                                    <Image class="icon-image" src="{{ favorite ? 'res://star_green' : 'res://star_white' }}" />
                                </GridLayout>
                                <StackLayout col="1" class="info-wrap">
                                    <Label text="{{ title }}" class="bold info-bigger" />
                                    <Label text="{{ director }}" class="info-orange" />
                                </StackLayout>
                            </GridLayout>
                        </ListView.itemTemplate>
                    </ListView>
                </ScrollView>
            </GridLayout>
            <ActivityIndicator busy="{{ isLoading }}" row="1" horizontalAlignment="center" verticalAlignment="center" />
        </GridLayout>
    </StackLayout>
</Page>
